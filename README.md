# beans

A coffee log.

## Local development

```sh
npm install
npm run dev
```

## Structure

- Journal entries live in `src/content/journal`.
- Gear lives in `src/content/gear`.
- Recipes live in `src/content/recipes`.

All content is written in Markdown so ChatGPT can append new entries easily.
