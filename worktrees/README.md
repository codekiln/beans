All git worktrees for this project should be created in subfolders of this directory.

Use the convention:

`${PROJECT_ROOT}/worktrees/${BRANCH_NAME}`

Preferred command:

`bd worktree create worktrees/${BRANCH_NAME} --branch ${BRANCH_NAME}`
